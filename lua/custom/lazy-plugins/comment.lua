return {

  -- Comment Out: Comment.nvim
  {
    'numToStr/Comment.nvim',
    config = function()
      require('Comment').setup({
        -- Optional: Customize the plugin's settings if needed
      })
    end
  },

  -- Add more plugins below as needed
}
